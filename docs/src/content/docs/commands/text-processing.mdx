---
title: Text Processing
description: sed, awk, cut, tr, uniq, sort, column, join, paste, comm, and diff commands.
---

# Text Processing

`sed`, `awk`, `cut`, `tr`, `uniq`, `sort`, `column`, `join`, `paste`, `comm`, and `diff` commands.
