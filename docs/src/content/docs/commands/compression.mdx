---
title: Compression
description: gzip, gunzip, zcat, and tar commands for compression and archiving.
---

# Compression

`gzip`, `gunzip`, `zcat`, and `tar` commands for compression and archiving.
