---
title: Encoding & Hashing
description: base64, md5sum, sha1sum, sha256sum, and file commands for encoding and integrity.
---

# Encoding & Hashing

`base64`, `md5sum`, `sha1sum`, `sha256sum`, and `file` commands for encoding and integrity.
